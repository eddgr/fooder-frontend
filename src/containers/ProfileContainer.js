import React from 'react'

// needs to check if logged in, if not redirect to login

export default class ProfileContainer extends React.Component {
  render() {
    return (
      <div>
        Profile here
      </div>
    )
  }
}
